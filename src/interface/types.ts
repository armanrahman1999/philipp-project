export interface Company {
  name: string;
  url?: string;
}

export interface LinkItem {
  text: string;
  url: string;
}

export interface WhitepaperInfo {
  title: string;
  fileSize: string;
  downloadUrl: string;
}
